ten en cuenta que ademas de todo esto de montar el proyecto, el favicon sólo se carga bien  si la carpeta images que contiene el archivo png está dentro de la carpeta dist entonces si a cada rato recreamos la carpeta dist perdemos la carpeta images entonces ¿qué conviene? ¿podemos vincular el index que está dentro de la carpeta dist de una manera correcta al png de la carpeta images? ¿o se puede hacer que dentro de las acciones automatizadas de github actions se repita cada vez volver a copiar la carpeta images con su png dentro de la carpeta dist cada vez que esta se vuelva a crear? ¿es un buen enfoque o convendría vincular de una sola vez el index que está dentro de la carpeta dist al png que está dentro de la carpeta images que a su vez está fuera de la carpeta dist?